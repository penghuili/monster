import { FollowingStatusPipe } from './following-status.pipe';

describe('FollowingStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FollowingStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
