import { LineHeightDirective } from './line-height.directive';

describe('LineHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new LineHeightDirective();
    expect(directive).toBeTruthy();
  });
});
