import { TextAlignDirective } from './text-align.directive';

describe('TextAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAlignDirective();
    expect(directive).toBeTruthy();
  });
});
