import { TimeRangeTypePipe } from './time-range-type.pipe';

describe('TimeRangeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeRangeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
