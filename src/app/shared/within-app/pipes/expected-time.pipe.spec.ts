import { ExpectedTimePipe } from './expected-time.pipe';

describe('ExpectedTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ExpectedTimePipe();
    expect(pipe).toBeTruthy();
  });
});
